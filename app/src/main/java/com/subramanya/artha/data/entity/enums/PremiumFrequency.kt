package com.subramanya.artha.data.entity.enums

/** Cadence on which an Insurance premium falls due. SINGLE = one-time policy. */
enum class PremiumFrequency {
    MONTHLY,
    QUARTERLY,
    HALF_YEARLY,
    YEARLY,
    SINGLE,
}
